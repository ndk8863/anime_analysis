CREATE TABLE public.m_s_user_details(
    mal_id integer,
    gender VARCHAR(50),
    birthday DATE,
    location VARCHAR(255),
    joined TIMESTAMP,
    days_watched FLOAT,
    mean_score FLOAT,
    watching FLOAT,
    completed FLOAT,
    on_hold FLOAT,
    dropped FLOAT,
    plan_to_watch FLOAT,
    total_entries FLOAT,
    rewatched FLOAT,
    episodes_watched FLOAT
);